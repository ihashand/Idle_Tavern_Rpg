import 'package:flutter/material.dart';

class KitchenScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kitchen'),
      ),
      body: Center(
        child: Text('This is the Kitchen Screen'),
      ),
    );
  }
}
