import 'package:flutter/material.dart';

class RoomsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rooms'),
      ),
      body: Center(
        child: Text('This is the Rooms Screen'),
      ),
    );
  }
}
