import 'package:flutter/material.dart';

class WarehouseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Warehouse'),
      ),
      body: Center(
        child: Text('This is the Warehouse Screen'),
      ),
    );
  }
}
