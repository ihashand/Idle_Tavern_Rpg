import 'package:flutter/material.dart';

class WoodStorageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wood Storage'),
      ),
      body: Center(
        child: Text('This is the Wood Storage Screen'),
      ),
    );
  }
}
